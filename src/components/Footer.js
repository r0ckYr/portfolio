import SocialLinks from "./SocialLinks";

const Footer = () => {
  return (
    <footer className="w-full bg-transparent backdrop-blur-sm py-12">
      <SocialLinks />
    </footer>
  );
};

export default Footer;
